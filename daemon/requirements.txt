# Developed and run on Python 3.14. torch's CPU wheel is enough to serve the
# default bge-small model; pick the ROCm or CUDA build only if you want GPU
# embedding. This file documents daemon rebuildability; it is not an isolation
# venv mandate.
#
# Optional: onnxruntime, only when PENSIVE_V3_ONNX_MODEL points at an ONNX
# export of the embedding model. Optional: openai, only when
# PENSIVE_V3_OPENAI_MODEL is set.
torch
sentence-transformers
numpy
threadpoolctl>=3
usearch>=2.25
mcp
starlette
uvicorn
setproctitle
