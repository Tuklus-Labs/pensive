# Loudness Audit: scoped recall candidate generation

AST audit found 21 assertions and zero missing assertion messages.

Every assertion:

- names a `scoped-recall` invariant or a setup-power condition;
- includes observed IDs, hits, results, scores, or call counts;
- carries a message unique enough to locate the failed rule;
- states the rule in present tense.

Exemptions: none.
