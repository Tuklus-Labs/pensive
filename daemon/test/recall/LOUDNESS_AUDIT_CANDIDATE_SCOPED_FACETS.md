# Loudness Audit: candidate-scoped facet boosts

AST inspection found 18 assertions and zero assertions without messages in
`test_candidate_scoped_facets.py`.

Every assertion message:

- names the candidate facet, liveness, stage-order, boost-order, trust-evidence,
  payload, token, confidence, plan, or empty-pool invariant;
- includes the relevant IDs, result rows, hints, payload, SQL, or observed state;
- uses a distinct phrase that can be searched in a test log;
- states the rule in the present tense.

Exemptions: none.

