# Loudness Audit: bounded exact memory selection

AST inspection found 19 assertions and zero assertions without explicit
messages in `test_exact_memory_selection.py`.

Every message names the boundary, scope, class-routing, cache-routing,
transition-limit, physical-length, selected-index, add, remove, retirement, or
allowed-ID invariant. Messages include the population, kind scope, selected
type, allowed IDs, cache calls, row counts, or returned hits needed for diagnosis.

Exemptions: none.
